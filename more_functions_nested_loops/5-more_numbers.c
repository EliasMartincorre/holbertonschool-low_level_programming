#include "main.h"
/**
* more_numbers -  10 times table
*
*
*/
void more_numbers(void)
{
	int den, mult, in, a;

	for (den = 0; den <= 9; den++)
	{
	for (mult = 48; mult <= 57; mult++)
	{
	_putchar(mult);

	}
	for (a = 49; a == 49; a++)
	{
	for (in = 48; in <= 52; in++)

	{
	_putchar((a) && (in));
	}
	}
	_putchar('\n');
	}
}
