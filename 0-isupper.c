#include "main.h"
/**
* _isupper - Check for uppercase character.
*
* @c: variable uppercase
* Return: 1 if uppercase
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
