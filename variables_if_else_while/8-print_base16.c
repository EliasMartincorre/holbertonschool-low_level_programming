#include <stdio.h>
/**
* main - Smile in the mirror .
*
*Return: return 0
*/
int main(void)
{
int a = '0';
char b = 'a';
for (a = '0'; a <= '9'; a++)
putchar('a');
for (b = 'a'; b <= 'f'; b++)
putchar('b');
putchar('\n');
return (0);
}
